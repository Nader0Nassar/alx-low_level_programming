#include "main.h"

/**
 * mul - gets the product of two numbers
 * @a: The 1st number
 * @b: The 2nd number
 *
 * Return: On success 1.
 * On error, -1 is returned, and errno is set appropriately.
 */
int mul(int a, int b)
{
	return (a * b);
}
